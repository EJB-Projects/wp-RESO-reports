<?xml version="1.0" encoding="UTF-8"?>
<project name="module_webapplication2(ii)" default="compile.module.webapplication2(ii)">
  <dirname property="module.webapplication2(ii).basedir" file="${ant.file.module_webapplication2(ii)}"/>
  
  <property name="module.jdk.home.webapplication2(ii)" value="${project.jdk.home}"/>
  <property name="module.jdk.bin.webapplication2(ii)" value="${project.jdk.bin}"/>
  <property name="module.jdk.classpath.webapplication2(ii)" value="${project.jdk.classpath}"/>
  
  <property name="compiler.args.webapplication2(ii)" value="-encoding UTF-8 -source 9 -target 9 ${compiler.args}"/>
  
  <property name="webapplication2(ii).output.dir" value="${module.webapplication2(ii).basedir}/out/production/WebApplication2(II)"/>
  <property name="webapplication2(ii).testoutput.dir" value="${module.webapplication2(ii).basedir}/out/test/WebApplication2(II)"/>
  
  <path id="webapplication2(ii).module.bootclasspath">
    <!-- Paths to be included in compilation bootclasspath -->
  </path>
  
  <path id="webapplication2(ii).module.production.classpath">
    <path refid="${module.jdk.classpath.webapplication2(ii)}"/>
  </path>
  
  <path id="webapplication2(ii).runtime.production.module.classpath">
    <pathelement location="${webapplication2(ii).output.dir}"/>
  </path>
  
  <path id="webapplication2(ii).module.classpath">
    <path refid="${module.jdk.classpath.webapplication2(ii)}"/>
    <pathelement location="${webapplication2(ii).output.dir}"/>
  </path>
  
  <path id="webapplication2(ii).runtime.module.classpath">
    <pathelement location="${webapplication2(ii).testoutput.dir}"/>
    <pathelement location="${webapplication2(ii).output.dir}"/>
  </path>
  
  
  <patternset id="excluded.from.module.webapplication2(ii)">
    <patternset refid="ignored.files"/>
  </patternset>
  
  <patternset id="excluded.from.compilation.webapplication2(ii)">
    <patternset refid="excluded.from.module.webapplication2(ii)"/>
  </patternset>
  
  
  <target name="compile.module.webapplication2(ii)" depends="compile.module.webapplication2(ii).production,compile.module.webapplication2(ii).tests" description="Compile module WebApplication2(II)"/>
  
  <target name="compile.module.webapplication2(ii).production" depends="register.custom.compilers" description="Compile module WebApplication2(II); production classes"/>
  
  <target name="compile.module.webapplication2(ii).tests" depends="register.custom.compilers,compile.module.webapplication2(ii).production" description="compile module WebApplication2(II); test classes" unless="skip.tests"/>
  
  <target name="clean.module.webapplication2(ii)" description="cleanup module">
    <delete dir="${webapplication2(ii).output.dir}"/>
    <delete dir="${webapplication2(ii).testoutput.dir}"/>
  </target>
</project>